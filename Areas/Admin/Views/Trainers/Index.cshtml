@model IEnumerable<StaffManager.Models.Trainer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    <div class="ms-md-auto pe-md-3 d-flex align-items-center col-md-3">
        <div class="input-group input-group-outline">
            <label class="form-label">Search</label>
            @Html.TextBox("Search", null, new
            {
                @type = "text",
                @class = "form-control"

       })
            <input type="submit" value="Searh">
        </div>
    </div>
}


<p>
    @Html.ActionLink("Create New", "Create", null, new
{
    @class = "btn btn-primary"
})

</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.User.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.User.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new
           {
               @class = "btn"
           })
                @Html.ActionLink("Details", "Details", new { id = item.Id }, new
           {
               @class = "btn"
           })
                @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new
           {
               @class = "btn"
           })
            </td>
        </tr>
    }

</table>
